[build]
  command = "NODE_OPTIONS=--openssl-legacy-provider npm run build"
  publish = "build"

[build.environment]
  SKIP_PREFLIGHT_CHECK = "true"
  NODE_VERSION = "16.20.0"  # Specified an exact version to avoid issues

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
